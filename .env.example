# OCR_ENGINE may be auto, paddle, or tesseract.
# auto tries PaddleOCR first and falls back to Tesseract if PaddleOCR fails.
OCR_ENGINE=auto
# Run PP-Structure only after a successful PaddleOCR result.
PADDLE_ENABLE_STRUCTURE=false
